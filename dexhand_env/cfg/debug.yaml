# @package _global_
# Debug configuration with verbose logging and small scale

defaults:
  - config
  - base/debug
  - base/test
  - _self_

env:
  render: true

training:
  maxIterations: 100  # Override for shorter debug sessions
